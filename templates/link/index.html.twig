{% extends 'base.html.twig' %}

{% block title %}Hello 1!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello 1! ✅</h1>
{{ form_start(form) }}
{{ form_widget(form) }}
{{ form_end(form) }}


    <table>
        {% for row in  array_link|batch(3, 'No item') %}
            <tr>
                {% for column in row %}
                    <td>{{ column }}</td>
                {% endfor %}
            </tr>
        {% endfor %}
    </table>

    <h1>Members</h1>
    <ul>
        {% for user in table_link %}
            <li>{{ user }}</li>
        {% endfor %}
    </ul>


    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'src/Controller/LinkController.php'|file_link(0) }}">src/Controller/LinkController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'templates/link/index.html.twig'|file_link(0) }}">templates/link/index.html.twig</a></code></li>
    </ul>
</div>
{% endblock %}
